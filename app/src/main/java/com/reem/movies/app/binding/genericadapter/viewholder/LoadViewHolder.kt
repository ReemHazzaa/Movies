package com.reem.movies.app.binding.genericadapter.viewholder

import androidx.databinding.ViewDataBinding
import com.reem.movies.app.binding.genericadapter.viewholder.BaseViewHolder

class LoadViewHolder(binding: ViewDataBinding) : BaseViewHolder(binding){

    override fun onBind(`object`: Any, position: Int) {
        super.onBind(`object`, position)
    }
}